<!-- 0. Make sure the addInstruction executess apply correctly for all methods: -->
1. Write unit tests for all resize methods:
    * build test for multiple, different images
2. Make an ar, pad, rt function
3. Fix the regex
4. Unit tests for regex
