<!-- 1. Write unit tests for all resize methods:
    * build a script that will create img snapshot + hash into json tests suites -->
2. Make an ar, pad, rt function
3. Fix the regex:
    * Unit tests for regex
4. Clean up parser: 
    * write unit test for parser
5. Implement the chain:: fn logic 
6. Write units test for zoom

rs(600,_),ar(4,3,fit:in)
<!-- 0. add the fit param to ar -->
<!-- 1. add a function inside the image state that gives the aspect ratio -->
2. update resolvers to user the function

extr - prewith 
rs with pad - post width
rs - width

